# --- dependencies ---
/node_modules

# --- env files ---
.env
.env.local
.env.*.local

# package-lock.json
package-lock.json

# --- logs ---
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- user-uploaded / generated content ---
# Keep the folder structure but ignore what users actually upload.
uploads/avatars/*
uploads/booking-progress/*
uploads/chat-media/*
uploads/requirements/*
uploads/tmp-chunks/*
!uploads/avatars/.gitkeep
!uploads/booking-progress/.gitkeep
!uploads/chat-media/.gitkeep
!uploads/requirements/.gitkeep
!uploads/tmp-chunks/.gitkeep

# --- coverage / testing ---
/coverage

# --- editor / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- misc ---
*.pid
*.seed
*.pid.lock
.node_repl_history